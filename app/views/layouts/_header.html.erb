<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "班级管理系统", root_path, :class => "navbar-brand" %>
    </div>

    <% if logged_in? %>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <% if current_user.admin? %>
      <ul class="nav navbar-nav">
        <%= nav_link_to "班级管理", klasses_path %>
        <%= nav_link_to "学生管理", users_path %>
      </ul>
      <% end %>

      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= @current_user.name %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "个人信息", user_path(@current_user) %></li>
            <li><%= link_to "账号管理", edit_user_path(@current_user) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "登出", logout_path, method: "delete" %></li>
          </ul>
        </li>
      </ul>
      <form class="navbar-form navbar-right" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="姓名">
        </div>
        <button type="submit" class="btn btn-default">搜索</button>
      </form>
      <% else %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "登入", login_path %></li>
      </ul>
      <% end %>
      
</div><!-- /.navbar-collapse -->

  </div><!-- /.container-fluid -->
</nav>
